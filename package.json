{
  "name": "archive-world-downloader-docs",
  "type": "module",
  "version": "0.0.1",
  "license": "LGPL-3.0-or-later",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "gate": "npm run check && npm run build 2>&1 | grep -qF 'All internal links are valid.' && ! grep -rn '—' src/content/docs && ! grep -rniE '\\bcaptur(e|es|ed|ing)\\b' src/content/docs && test -z \"$(grep -rLE '^description: *\\S' src/content/docs)\" && ! grep -rn '^# ' src/content/docs && echo GATE_OK",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.1.6",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "starlight-links-validator": "^0.25.2",
    "typescript": "^6.0.3"
  },
  "allowScripts": {
    "esbuild": true,
    "fsevents": false
  }
}
