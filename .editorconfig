# Mechanical formatting source of truth for the docs site.
# No linter runs over this tree, so what editors read here is the whole rule.

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2
